# AKSOS SVG Engineering
Before implementation: viewBox → semantic regions → node coordinates → label safe zones → endpoints → responsive variants → animation states.

Never use random positions, force simulation, global overflow hiding, or label-driven accidental geometry.

When mobile density becomes too high, simplify the graph rather than shrinking it.
